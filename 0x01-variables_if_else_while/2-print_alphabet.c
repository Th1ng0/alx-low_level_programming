#include <stdio.h>

/**
 * main - prints the alphabet in lowercase,
 * followed by a new line 5
 * Return: Always 0 (Success)
 */
int main(void)
	char c = 'a' ;
{
	for (c <= 'z')
	{
		putchar(ch);
		c++;
	}

	putchar('\n');
	return (0);
}
